# UAV–机械臂树枝修剪系统阶段 B：V2 完整仿真平台阶段性总结

> **在阶段 A 已完成 UAV–Arm 最小可运行验证的基础上，本阶段进一步面向“树枝修剪任务”重构机械臂与末端执行器，补充目标树枝、接触切割、RGB 相机和 MID360 激光雷达等模块，形成一套可继续开展感知、规划与协同控制研究的完整仿真平台。**

---

## 1. 阶段 B 的目标

阶段 A 的核心任务是验证：

```text
无人机能否搭载机械臂飞行？
机械臂能否在悬停状态下正常动作？
```

阶段 B 靠近真实任务：

```text
建立面向树枝修剪任务的完整仿真场景？
        ↓
机械臂结构向伐枝作业优化？
        ↓
链锯末端能与树枝的接触与切割逻辑？
        ↓
Camera + MID360 形成后续目标感知所需的数据基础？
```

本阶段的核心工作可以概括为：

> **从 UAV–Arm 飞行 MVP 升级为具备机械作业、切割交互和双传感器感知能力的任务级仿真平台。**

---

## 2. 平台总体结构

当前 V2 平台由以下部分组成：

```text
PX4 SITL + X500 UAV
        │
        ├── 3DOF 机械臂
        │     ├── J2 Shoulder Pitch
        │     ├── J3 Elbow Pitch
        │     └── J4 Wrist Roll
        │
        ├── 电动链锯末端
        │
        ├── RGB Camera
        │
        ├── MID360-like 3D LiDAR
        │
        └── ROS2 / Gazebo / PX4 联调
```

---

## 3. 本阶段主要完成内容

### 3.1 机械臂结构重构：由通用验证结构转向前向伐枝结构

进入阶段 B 后，为更贴近实际伐枝任务，对机械臂进行了重新布置：

- 去除独立 J1 Yaw；
- 将机械臂布置为机体前向作业；
- 使用 UAV_yaw 代替机械臂基座水平旋转；
- 保留 J2、J3 两个 Pitch 关节；
- 使用 J4 Wrist Roll 调整链锯滚转姿态。

机械臂不再设置独立的 J1 Yaw，由 UAV 自身 Yaw 完成水平方向调整，因此作业自由度为：

\[
[\ {UAV}_{yaw},\ J_2,\ J_3,\ J_4]
\]

当前主要逻辑关节范围为：

| 关节 | 功能 | 逻辑角范围 |
|---|---|---:|
| J2 | Shoulder Pitch | -165° ~ +15° |
| J3 | Elbow Pitch | -150° ~ +150° |
| J4 | Wrist Roll | -180° ~ +180° |

---

### 3.2 将圆锯方案进一步改为链锯式末端

为更贴近树枝修剪任务，本阶段重新设计了末端切割结构。

当前链锯采用：

```text
固定链锯本体 / 导板
        +
内部旋转驱动部分
        +
导板周边切割接触区域
```

优化使外观和任务逻辑更加接近真实电动链锯，在导板周围建立切割接触区域，用于检测链锯是否真正与目标枝条接触。

---

### 3.3 建立目标树枝与自动切割逻辑

在 Gazebo 中加入专门的测试树与目标枝条。

目标枝条由两部分组成：

```text
树体（数值）固定部分
        +
可脱落目标枝条（红色标记）
```

其中目标枝条外段设置为动态模型，可以在切割完成后自动脱落。

切割逻辑综合判断：

```text
链锯的转速
     +
目标接触状态
     +
有效接触持续时间
```

当前测试逻辑中：

- 低转速状态不触发有效切割（800 rpm）；
- 达到有效转速后，连续保持接触；
- 达到有效切割时间阈值后，目标枝条自动脱落；
- 设置短时间接触间隙容忍，避免瞬间接触抖动导致进度清零。

实际已验证：

```text
约 500 rpm：不能完成切割
约 1000 rpm：能够完成切割
```

---

## 4. RGB Camera 感知平台

### 4.1 相机安装与参数

UAV 前下部安装 RGB Camera，用于后续目标枝条识别。

当前主要参数：

| 参数 | 当前设置 |
|---|---:|
| 分辨率 | 1280 × 720 |
| 帧率 | 30 Hz |
| 水平 FOV | 90° |
| 安装位置 x | 0.120 m |
| 安装位置 z | -0.025 m |
| 安装俯角 | 约 25° |

ROS2 主要话题：

```text
/camera/image_raw
/camera/camera_info
```

当前相机可稳定输出图像与内参数据。

### 4.2 Camera FOV 可视化

为便于在 Gazebo 中观察相机覆盖范围，额外建立了 Camera FOV 可视化模型。

Camera FOV 用于：

```text
观察安装角度
判断目标是否进入视场
后续分析 Camera / LiDAR 共同视场
```

并通过 Gazebo visibility mask 处理，使辅助 FOV 模型：

```text
Gazebo 主界面可见
Camera 图像中不可见
MID360 点云中不可见
```

避免可视化模型污染真实传感器数据。

---

## 5. MID360 三维激光雷达平台

### 5.1 MID360 外观与安装

根据 Livox MID360 官方模型装换并导入到了 Gazebo，并安装在 UAV 前上部。

当前主要安装参数：

| 参数 | 当前设置 |
|---|---:|
| 安装 x | 0.099 m |
| 安装 z | 0.010 m |
| 安装俯角 | 20° |
| 雷达本体相对安装座 z | 0.033 m |
| 雷达本体 Yaw | 180° |

模型用于在 Gazebo 中提供较接近真实设备的外观和安装关系。

### 5.2 GPU LiDAR 参数

Gazebo 中采用 GPU LiDAR 模拟 MID360 点云。

当前主要参数：

| 参数 | 当前设置 |
|---|---:|
| 水平 FOV | 360° |
| 垂直 FOV | -7° ~ +52° |
| 水平采样数 | 360 |
| 垂直采样数 | 60 |
| 点云频率 | 10 Hz |
| 近距离 | 0.1 m |
| 最远距离 | 40 m |
| 理论点率 | 约 216,000 points/s |

说明：

> 当前 Gazebo 使用规则角度网格近似 MID360 扫描，并未模拟真实 MID360 的非重复扫描轨迹。对于本阶段的功能验证，该近似已经能够满足需求。

ROS2 点云话题：

```text
/mid360/points
```

测试：频率稳定在约 10 Hz。

### 5.3 MID360 FOV 可视化

在 Gazebo 中额外加入 MID360 官方视场模型，用于直观显示雷达的大致覆盖范围。

同样通过 visibility mask 保证：

```text
Gazebo 中可以看到 FOV
Camera 不会拍到 FOV
MID360 不会扫描自己的 FOV
```

同时兼顾演示效果和传感器数据纯净度。

---

## 6. TF 与 ROS2 数据链

Camera 与 MID360 均以 UAV `base_link` 为统一参考。

MID360 当前主要 TF 链为：

```text
base_link
    ↓
mid360_mount_link
    ↓
mid360_link
    ↓
mid360_gpu_lidar
```

Camera 也具有固定安装外参。

因此后续感知算法统一转换至：

```text
base_link
```

进行计算。

这为下一阶段的：

```text
RGB–LiDAR 融合
目标三维定位
枝条方向估计
UAV主动重定位
```

提供了坐标基础。

---

## 10. 后续研究问题

### 10.1 目标枝条不能只用一个空间点表示

后续感知除了目标位置，还需要获得：

```text
枝条中心位置
枝条主方向
枝条长度
枝条半径
切割点
切割平面
```

因此下一阶段需要对目标枝条进行三维几何参数化。

### 10.2 Camera 与 MID360 需要真正进行数据融合

目前两个传感器的数据通路已经建立，但尚未完成：

```text
RGB目标识别
        +
LiDAR三维点云
        ↓
目标枝条3D点云
```

这将是下一阶段最直接的工作。

### 10.3 “机械臂能够到达”不等于“适合切割”

后续任务规划不能只考虑可达性，还需要考虑：

- 关节余量；
- 奇异性；
- 机械臂与 UAV 碰撞；
- 机械臂伸展程度；
- Camera / LiDAR 可见性；
- 链锯切割姿态。

因此工作空间应该发展为：

```text
可达工作空间
     ↓
安全工作空间
     ↓
优选工作空间
```

### 10.4 UAV 应该融为机械臂任务一同规划

如果目标：

```text
机械臂够不到
     或
虽然够得到但构型不理想
```

则不应简单判定任务失败，而应考虑：

```text
UAV 主动重定位
     ↓
更新目标相对位置
     ↓
重新规划机械臂
```

后续可以进一步研究 UAV 与机械臂之间的任务级协同。

---

## 11. 下一阶段计划：进入阶段 C——目标感知

下一阶段不立即追求复杂林区环境下的高鲁棒性感知，而是首先完成 Gazebo Demo 的基本感知闭环。

阶段 C 第一项工作暂定为：

> **基于 RGB–LiDAR 融合的目标枝条三维感知与几何参数提取。**

初期将目标枝条近似为圆柱：

\[
\mathcal{B}=\{\mathbf{p}_0,\mathbf{d},L,r\}
\]

其中：

- \(\mathbf{p}_0\)：枝条中心位置；
- \(\mathbf{d}\)：枝条主方向；
- \(L\)：枝条长度；
- \(r\)：枝条半径。

计划路线：

```text
RGB Camera
   ↓
目标枝条识别
   ↓
获得目标 Mask
   ↓
MID360 点云与图像关联
   ↓
提取目标枝条 3D 点云
   ↓
简单圆柱（近似）参数估计
   ↓
p0 + d + L + r 参数获取
   ↓
RViz 内的圆柱模型可视化
```

---

## 12. 阶段 B 总结

阶段 B 的核心成果可以概括为：

> **在阶段 A UAV–Arm 飞行 MVP 的基础上，完成面向树枝修剪任务的 V2 仿真平台重构，形成了包含前向 3DOF 机械臂、链锯末端、目标树枝自动切割、RGB Camera、MID360 三维雷达、TF 与 ROS2 数据链的完整任务级仿真环境。**

当前系统已经具备：

```text
无人机飞行
        +
机械臂作业
        +
链锯切割
        +
目标树枝交互
        +
RGB图像
        +
3D点云
```

等后续研究所需的主要基础能力。

因此，本阶段的主要意义不是已经完成了自主伐枝，而是：

> **已经将“能够飞起来的 UAV–Arm 原型”推进为“可以继续开展自主感知、任务规划、安全评价和协同控制研究的完整实验平台”。**

---