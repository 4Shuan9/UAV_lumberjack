# UAV_with_arm_MVP_阶段性总结

> **阶段目标：完成无人机搭载 3 自由度机械臂的最小飞行 MVP，验证“能飞、能悬停、机械臂能在空中动作，并且系统可稳定恢复”。**

---

## 1. 研究目标与当前阶段定位

**总体目标**：面向输电线路下方树枝修剪任务，构建一套“无人机 + 机械臂 + 末端切割工具”的空中作业系统。

此系统后续会涉及机械臂运动学、轨迹规划、视觉定位、末端切割、接触扰动以及无人机-机械臂协同控制等问题。

为了避免一开始把系统做得过于复杂，当前先设计并完成一个**最小飞行 MVP**：

1. 在 Gazebo + PX4 中建立 X500 与 3 自由度机械臂的组合模型；
2. 无人机能够正常起飞、悬停、动作；
3. ROS2 可以独立控制 J1、J2、J3 三个机械臂关节；
4. 机械臂在空中进行大角度动作时，无人机不会失控，并能够由 PX4 稳定悬停。

本阶段性目标已经完成。

---

## 2. 机械臂技术路线选择：成熟开源机械臂还是自研机械臂

在正式进入 UAV - Arm 协同控制之前，机械臂平台本身的技术路线需要先确定。
目前主要考虑两种方案：

### 2.1 移植成熟开源机械臂

方案一：选择比较成熟的开源机械臂，在现有基础上移植到无人机平台。

**优点**：
- 机械结构、控制系统和参数相对成熟；
- 通常已经具备较完整的位置控制、运动学模型和接口；
- 后续更容易接入轨迹规划、IK、MoveIt 等成熟工具链；
- 可以减少大量机械臂底层控制和基础功能开发工作。

但**问题**也比较明显：
- 现有机械臂大多针对固定基座设计，原始仿真环境与 UAV/PX4 环境并不一致；
- 需要重新移植 Gazebo 模型、ROS2 接口和控制程序；
- 内部程序、接口定义、参数配置需要重新查阅手册和源码；
- 机械臂原有质量、尺寸、惯量和关节范围未必适合无人机挂载；
- 即使机械臂本身控制成熟，安装到 floating-base 无人机后，其原有控制参数也不一定可以直接使用。

### 2.2 自研简单机械臂

方案二：自研的简化机械臂，并逐步完善。

**优点**：
- 可以完全按照无人机载荷、尺寸和树枝修剪需求定制；
- 机械结构、质量、惯量、关节范围都可以直接控制；
- 与当前 PX4 + Gazebo + ROS2 系统完全兼容；
- 对后续动力学分析和控制算法研究更加透明。

**缺点**：
- 运动控制系统不成熟，需要自己逐步开发；
- 位置控制、轨迹平滑、运动学、IK、安全约束等都需要重新建立；
- 随着机械臂功能变复杂，工程开发工作量会快速增加。

目前自研机械臂已经完成了最小飞行 MVP，因此证明了整套 UAV - Arm 技术链路的可行性！

**下一阶段准备重点评估一个成熟开源机械臂的移植可行性，再决定后续是以成熟开源机械臂作为主要研究平台，还是继续沿用自研机械臂并针对树枝修剪任务进行定制。**

---

## 3. 开发流程：从单关节逐步叠加到空中机械臂

整个开发过程采用“逐级叠加”的方式，每一步只增加一个新的复杂度，先保证当前层级稳定，再进入下一层。

### Step 1：单关节模型验证

首先没有直接搭建完整机械臂，而是在 Gazebo 中建立单自由度旋转关节，主要验证：

- SDF 关节定义是否正确；
- 关节轴方向、角度范围是否符合预期；
- Gazebo 关节位置控制器能否正常工作。

这一步主要解决最基础的问题：**关节能否按照预期运动。**

### Step 2：3 自由度机械臂独立模型

在单关节验证通过后，扩展到 3 自由度机械臂：

- J1：基座 Yaw；
- J2：肩部 Pitch；
- J3：肘部 Pitch。

这一阶段主要完成：

- 机械结构；
- 质量与惯量；
- 关节运动方向；
- 关节角度范围；
- 基本位置控制。

这个阶段机械臂仍然是固定基座，主要目标是先把机械臂自身的运动控制跑通。

### Step 3：ROS2 与 Gazebo 联调

之后通过 `ros_gz_bridge` 建立 ROS2 与 Gazebo 之间的命令和反馈通道，并开发 `arm_controller`。

目前控制器支持：

```text
j1 <deg>
j2 <deg>
j3 <deg>
home
status
```

同时加入：

- 关节状态反馈；
- 位置误差判断；
- 关节速度判断；
- BUSY 状态；
- 到位判定；
- HOME 回零。

这样机械臂不再只是 Gazebo 内部可动，而是形成了一个可以由 ROS2 上层控制的完整接口。

### Step 4：机械臂安装到 X500

随后将机械臂安装到 PX4 的 X500 模型下方，并针对飞行场景重新设计机械臂。

这里没有直接使用前期独立机械臂的质量和尺寸，而是重新进行了轻量化，因为机械臂的：

```text
质量
长度
惯量
安装位置
运动姿态
```

都会直接影响整机重心、姿态响应和飞行稳定性。

也是从这一步开始，机械臂不再是一个“固定基座系统”，而变成了一个安装在 floating-base 无人机上的动态负载。

### Step 5：PX4 飞行验证

最后建立自定义 `x500_lumberjack` 模型，由 PX4 SITL 接管。

完成了以下验证：

- QGC 正常连接；
- PX4 ARM、螺旋桨响应正常；
- X500 搭载机械臂正常起飞、悬停；
- ROS2 在飞行过程中可以控制 J1/J2/J3；
- J1、J2、J3 进行较大角度动作后，PX4 可以快速恢复悬停。

最终测试中：

```text
J1：完成较大范围正反向旋转
J2：完成约 ±45° 动作
J3：完成约 ±60° 动作
```

机械臂动作时无人机会出现一定瞬态姿态变化和位置偏移，但 PX4 可以迅速调节并重新稳定。

因此，“**无人机能带机械臂飞起来，并在空中操作机械臂**”这一最小目标已经完成。

---

## 4. MVP 开发过程中暴露出的关键问题

完成最小飞行 MVP 的意义不仅是证明系统“能飞、能动”，更重要的是暴露出了后续真正需要研究的问题。

### 4.1 机械臂安全工作空间不能简单采用独立关节限位

目前机械臂控制仍然使用：

```text
J1：[-180°, +180°]
J2：[-90°, +90°]
J3：[-90°, +90°]
```

这种独立关节角度限制。

但实际安装到无人机后发现：

> **机械臂是否安全并不只取决于某一个关节角度，而取决于多个关节共同形成的空间构型。**

例如：

- 当 J1 指向某个电机方向时，J2 的某些角度可能使机械臂进入螺旋桨或机架附近；
- 当 J1 转到另一侧后，同样的 J2 角度又可能完全安全；
- 某个 J3 角度在机械臂伸展状态下可能危险，但在折叠状态下可能没有问题。

因此后续需要描述的是：

```text
(q1, q2, q3) -> 是否安全
```

而不是三个互相独立的：

```text
q1_min < q1 < q1_max
q2_min < q2 < q2_max
q3_min < q3 < q3_max
```

后续安全工作空间还需要综合考虑：

- 螺旋桨区域；
- 电机与机架；
- 起落架；
- 地面；
- 末端切割工具；
- 树枝；
- 输电线路等外部障碍物。

因此机械臂安全约束最终需要从“关节限位”升级到“**构型空间安全约束 / 工作空间约束**”。

---

### 4.2 无人机与机械臂的位置不能完全分开规划

目前的基本工作方式仍然是：

```text
无人机先到达并悬停
   +
机械臂再单独动作
```

这种方法适合最小 MVP 验证，但不适合最终树枝修剪任务。

后续希望系统的输入不再是“无人机飞到哪里、机械臂转多少度”两套独立指令，而是直接给出一个**待切割树枝的目标坐标**。系统根据该目标一次性完成任务级规划：

```text
输入：待切割树枝目标坐标
        ↓
规划无人机接近路径
        ↓
选择树枝附近的安全悬停 / 作业位姿
        ↓
根据该无人机位姿自动求解机械臂可达构型
        ↓
规划机械臂末端到达切割点
```

也就是说，真正需要联合考虑的是：

```text
无人机机体位置、姿态
   +
机械臂关节构型
   +
末端切割点
```

共同决定一个完整的作业方案。

实际执行时仍然可以采用“无人机先到安全位置，再展开机械臂”的分阶段方式，但**规划层面应该从目标点出发，同时确定合适的无人机作业位姿和机械臂可达构型**，而不是无人机和机械臂各自独立规划后再简单拼接。

这个问题不仅涉及末端几何可达性，还需要同时考虑：

- 无人机悬停和姿态控制裕量；
- 机械臂安全构型；
- 机械臂奇异位形；
- 整机重心偏移；
- 螺旋桨和障碍物安全距离；
- 机械臂动作对无人机的扰动；
- 作业效率和能耗。

因此后续目标可以概括为：**给定树枝切割坐标，自动规划无人机安全接近位置与机械臂末端到达轨迹。**

---

### 4.3 分离控制系统之间存在明显动力学耦合

本阶段调试过程中最明显的问题之一，就是：

> **机械臂动作会直接影响无人机飞行状态。**

早期空中测试时，机械臂一运动，无人机出现明显下沉、横向漂移，严重时会持续漂移，需要人工重新调整。

可能的耦合来源包括：

1. **机械臂构型变化造成整机重心偏移；**
2. **机械臂加减速产生反作用力和反作用力矩；**
3. **机械臂控制器高频振荡通过安装结构传递到机体；**
4. **机体振动进一步影响 PX4 IMU 与 EKF 状态估计。**

实际调试中，J2、J3 曾在 400 Hz 仿真步长下产生明显的高频正负速度翻转。

视觉上机械臂看起来已经接近目标位置，但内部关节速度仍持续高速振荡。机械臂运动因此不断向无人机施加扰动，随后 PX4 出现：

```text
High Accelerometer Bias
```

并导致 QGC 无法正常进入 Ready 状态。

后续将 J2/J3 的控制方式调整为：

```text
P 控制
   +
physical joint damping
   +
控制输出限幅
```

同时清除异常保存的加速度计校准偏置。

之后：

- J2/J3 静止速度下降到数值噪声量级；
- PX4 加速度计偏置恢复正常；
- QGC 恢复 `Ready to Fly`；
- 空中大角度机械臂动作时，无人机可以稳定恢复。

这个现象说明：

> **机械臂控制器与无人机飞控虽然在程序上是两个分离系统，但在动力学上属于一个强耦合系统。**

这也是后续 UAV - Arm 协同控制的重要切入点。

---

### 4.4 多关节机械臂-无人机复合系统的调参问题

另外一个比较明显的问题是：

> **固定基座机械臂上调好的控制参数，安装到无人机以后并不能直接认为仍然适用。**

固定基座机械臂的基座不会运动，而无人机属于 floating base。机械臂施加力矩时，无人机机体本身也会产生响应，因此机械臂参数评价不能只看“关节位置误差是否最小”。

本阶段实际调试后，对机械臂控制策略也做了一个比较明确的取舍：

> **不再单纯追求高精度、极小稳态误差，而是在误差满足任务需求的前提下，优先保证机械臂动作平稳、减少高频振荡和对无人机的反作用。**

因此 J2/J3 最终采用了：

```text
P 控制
   +
关节物理阻尼
   +
控制输出限幅
```

这种方式会保留一定的稳态误差，但可以明显降低高频振荡和瞬时反作用力。对于 UAV-Arm 系统，这种“保留可接受误差、换取整体稳定性”的控制方式比一味追求极高关节精度更加合适。

因此后续调参需要同时考虑：

```text
关节位置误差
关节响应速度
关节运动加速度
高频振荡程度
无人机姿态扰动
无人机位置漂移
PX4 恢复时间
```

例如：

- 一组参数可以把关节误差压到 0.1°，但会让机械臂高频振荡、无人机明显偏航；
- 另一组参数保留约 0.5° 左右误差，但机械臂动作平稳、无人机扰动很小。

对于 UAV-Arm 系统，第二组参数反而更加合理。

目前 J1/J2/J3 均能够完成较大角度动作，并在动作结束后稳定保持。后续如果需要进一步提高精度，更优先考虑轨迹平滑、前馈补偿等方法，而不是重新通过提高增益去强行压缩误差。

---

## 5. 当前阶段成果

目前最小飞行 MVP 已经完成：

```text
X500 + 3DOF Arm 建模
ROS2 机械臂控制
PX4 SITL 接管
QGC 起飞 / 悬停
J1 / J2 / J3 空中动作
机械臂大角度动作
PX4 自动恢复稳定
关节高频振荡问题定位与解决
High Accelerometer Bias 定位与解决
飞行版关节参数定型
```

目前已经具备一个能够继续进行 UAV-Arm 研究的基础仿真平台。

本阶段最重要的结果不是单独把无人机和机械臂分别跑通，而是已经验证了：

> **无人机可以搭载机械臂稳定飞行，机械臂可以在悬停过程中完成较大角度动作，并且 PX4 可以对机械臂运动产生的扰动进行快速恢复。**

---

## 6. 下一阶段计划

下一阶段首先需要确定机械臂平台路线。

### 6.1 机械臂平台确定

重点评估一个成熟开源机械臂的移植，包括：

- Gazebo 模型；
- ROS2 接口；
- 关节控制系统；
- 运动学参数；
- 质量、尺寸与惯量；
- 是否适合无人机挂载；
- 是否方便后续末端工具和协同控制研究。

在完成评估后，再决定：

> **采用成熟开源机械臂作为后续主平台，还是继续使用自研机械臂并围绕树枝修剪任务进一步定制。**

### 6.2 轨迹平滑

当前关节命令本质上仍然是位置阶跃，例如：

```text
j2 45
```

相当于目标位置瞬间从当前角度变为 45°。

下一阶段需要引入：

```text
梯形速度轨迹
   或
S 曲线速度 / 加速度轨迹
```

对关节速度和加速度进行限制，进一步降低机械臂动作时的反作用冲击。

### 6.3 机械臂安全工作空间

从当前简单的独立关节限位，升级为：

```text
(q1, q2, q3) -> 安全 / 不安全
```

建立机械臂与：

- 电机；
- 螺旋桨；
- 机架；
- 起落架；
- 末端工具；
- 外部障碍物

之间的空间安全约束。

### 6.4 无人机-机械臂联合规划

后续希望把规划接口进一步上移到任务层。

系统输入一个待切割树枝的目标坐标后，自动完成：

```text
目标树枝坐标
   ↓
无人机接近路径规划
   ↓
附近安全作业位姿选择
   ↓
机械臂可达构型求解
   ↓
机械臂末端轨迹规划
   ↓
到达切割位置
```

因此联合规划的核心不是简单把“无人机路径”和“机械臂轨迹”串起来，而是**从同一个目标点出发，同时决定无人机停在哪里、机械臂采用什么构型到达目标**。

实际执行可以采用无人机先到达安全作业位置、再驱动机械臂接近目标的分阶段方式，但规划阶段需要同时考虑无人机安全、机械臂可达性和整体稳定性。

### 6.5 UAV - Arm 协同控制

针对：

- 重心变化；
- 机械臂反作用力矩；
- 机械臂运动扰动；
- 多关节耦合；
- 飞行姿态变化

进一步研究前馈补偿或更系统的 UAV-Arm 耦合控制方法。

在以上基础逐步稳定之后，再进一步加入：

```text
视觉目标定位
   ↓
末端切割工具
   ↓
树枝接触
   ↓
切割扰动
   ↓
完整树枝修剪任务
```

---

## 7. 总结

当前已经完成了 **UAV + 3DOF 机械臂最小飞行 MVP**，建立了一个完整的：

```text
Gazebo
   +
PX4 SITL
   +
QGC
   +
ROS2
   +
3DOF Arm
```

联合仿真与控制平台。

整个开发过程从单关节开始，逐步扩展到 3 自由度机械臂、ROS2 控制、X500 挂载，最后完成 PX4 空中动作验证。

在这一过程中，也明确暴露出了后续需要重点解决的几个问题：

1. 成熟开源机械臂移植与自研机械臂之间的技术路线选择；
2. 机械臂安全区域不能简单依靠独立关节限位；
3. 无人机位置与机械臂构型需要联合规划；
4. 无人机飞控与机械臂控制虽然程序上分离，但动力学上存在明显耦合；
5. floating-base 条件下，机械臂控制参数需要同时考虑机械臂性能与无人机扰动；
6. 后续需要从位置控制逐步发展到轨迹规划和 UAV-Arm 协同控制。

因此当前最小飞行 MVP 已经完成。

下一阶段将从“**能飞、能动**”进一步转向：

> **机械臂平台更加成熟、动作更加平滑、工作空间更加安全，并最终实现无人机与机械臂之间的联合规划和协同控制。**